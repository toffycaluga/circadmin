<% content_for :title, t("invitations.new.title") %>

<h1><%= t("invitations.new.heading") %></h1>

<%= render "form", invitation: @invitation %>

<br>

<div>
  <%= link_to t("invitations.new.back"), invitations_path %>
</div>
