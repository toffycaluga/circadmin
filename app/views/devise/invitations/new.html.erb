<%= form_for(User.new, as: :user, url: user_invitation_path, method: :post) do |f| %>
  <%= f.label :email %>
  <%= f.email_field :email, class: "form-control" %>

  <%= label_tag :role, "Role" %>
  <%= select_tag :role, options_for_select([["Admin", "admin"], ["Contador", "contador"], ["Representante", "representante"]]), class: "form-control" %>

  <%= f.submit "Enviar invitación", class: "btn btn-primary" %>
<% end %>
